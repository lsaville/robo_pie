<h1>All Pies</h1>

<% @items.each do |item| %>
  <p><%= item.title %></p>
  <img src="<%= item.image %>" alt="delicious item" />
  <p><%= item.description %></p>
  <p><%= item.price %></p>
  <%= button_to "Add To Cart", carts_path(item_id: item.id), class: "btn btn-danger" %>
<% end %>
