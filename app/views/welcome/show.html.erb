<div class='text-center'>
  <h1>Welcome</h1>
<% if logged_in? %>
  <%= button_to "Start Shopping!", items_path, method: :get, class: 'btn btn-primary' %>
<% else %>
  <%= button_to "Login", login_path, method: :get, class: 'btn btn-primary' %>
<% end %>
</div>
